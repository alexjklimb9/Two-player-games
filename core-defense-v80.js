// Core Defense v80 consolidated playable build
const canvas=document.getElementById('gameCanvas'),ctx=canvas.getContext('2d');
const startOverlay=document.getElementById('startOverlay'),endOverlay=document.getElementById('endOverlay'),startButton=document.getElementById('startButton'),restartButton=document.getElementById('restartButton');
const resultTitle=document.getElementById('resultTitle'),resultText=document.getElementById('resultText'),finalStats=document.getElementById('finalStats');
const C={bg:'#0b0f14',bg2:'#151b23',line:'rgba(235,240,245,.075)',blue:'#4fb8d8',rose:'#e06f8f',white:'#f8fafc',core:'#d7dde5',danger:'#d97757',green:'#8bd3a7',gold:'#f0b86e',purple:'#b78cff',rock:'#a8a29e'};
const q=new URLSearchParams(location.search),LV=parseInt(q.get('level')||'2',10),TARGET=({1:5,2:8,3:12,4:15,5:999999}[LV]||8),ENDLESS=LV===5;
const TYPES=[{name:'Pulse',color:C.blue,range:185,rate:.46,damage:1,kind:'bullet'},{name:'Beam',color:C.gold,range:230,rate:.92,damage:2,kind:'beam'},{name:'Freeze',color:C.purple,range:165,rate:1.1,damage:.5,kind:'slow'},{name:'Wave',color:'#34d399',range:155,rate:1.05,damage:.42,kind:'wave'}];
const ET=[{name:'small',color:'#facc15',hp:.65,sp:1.45,r:10,reward:2},{name:'medium',color:'#a8a29e',hp:1,sp:1,r:15,reward:3},{name:'large',color:'#f97316',hp:1.85,sp:.62,r:22,reward:5}];
const COST={1:15,2:25,3:42,4:70},BUILD=6,MAX=5,BOOST_CD=10,BOOST_TIME=4.8;
const game={running:false,timer:0,wave:0,waveTimer:0,waveActive:false,spawnLeft:0,spawnClock:0,spawnIndex:0,spawnPattern:[],kills:0,money:0,coreHp:8,maxHp:8,ring:0,selected:0,buildType:0,specialCd:0,slots:[],enemies:[],shots:[],sparks:[],input:{topLeft:false,topMid:false,topRight:false,bottomLeft:false,bottomMid:false,bottomRight:false}};
function resize(){canvas.width=innerWidth;canvas.height=innerHeight}addEventListener('resize',resize);resize();
const cx=()=>canvas.width/2,cy=()=>canvas.height/2,clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function rr(x,y,w,h,r){ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+w-r,y);ctx.quadraticCurveTo(x+w,y,x+w,y+r);ctx.lineTo(x+w,y+h-r);ctx.quadraticCurveTo(x+w,y+h,x+w-r,y+h);ctx.lineTo(x+r,y+h);ctx.quadraticCurveTo(x,y+h,x,y+h-r);ctx.lineTo(x,y+r);ctx.quadraticCurveTo(x,y,x+r,y);ctx.closePath();ctx.fill();ctx.stroke()}
function clearInput(){Object.keys(game.input).forEach(k=>game.input[k]=false)}
function empty(s){return !s||(s.type!==0&&!s.type)}
function makeSlots(){game.slots=[];for(let i=0;i<12;i++)game.slots.push({type:null,level:0,cd:0,flash:0,boost:0})}
function price(s){if(empty(s))return BUILD;if(s.level>=MAX)return 0;return COST[s.level]||70}
function waveText(){return 'Wave '+Math.max(1,game.wave||1)+'/'+(ENDLESS?'∞':TARGET)}
function spark(x,y,color,n=8){for(let i=0;i<n;i++){let a=Math.random()*6.283,s=35+Math.random()*150;game.sparks.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:1,size:1.5+Math.random()*3,color})}}
function slotPos(i){let inner=i>=6,idx=inner?i-6:i,off=inner?Math.PI/6:0,a=game.ring+idx*Math.PI*2/6-Math.PI/2+off,r=inner?Math.min(canvas.width,canvas.height)*.095+18:Math.min(canvas.width,canvas.height)*.14+28;return{x:cx()+Math.cos(a)*r,y:cy()+Math.sin(a)*r,a}}
function startGame(e){if(e){e.preventDefault();e.stopPropagation()}Object.assign(game,{running:true,timer:0,wave:0,waveTimer:1.2,waveActive:false,spawnLeft:0,spawnClock:0,spawnIndex:0,spawnPattern:[],kills:0,money:LV<=1?12:LV===2?10:8,coreHp:LV<=1?8:7,maxHp:LV<=1?8:7,ring:0,selected:0,buildType:0,specialCd:0,enemies:[],shots:[],sparks:[]});clearInput();makeSlots();startOverlay.classList.add('hidden');endOverlay.classList.add('hidden')}
function endGame(msg,win=false){if(!game.running)return;game.running=false;clearInput();resultTitle.textContent=win?'You Win':'Game Over';resultText.textContent=msg;finalStats.innerHTML=`Waves Cleared: ${Math.max(0,game.wave-1)}${ENDLESS?'':'/'+TARGET}<br>Enemies Destroyed: ${game.kills}<br>Money: $${game.money}<br>Core Health: ${game.coreHp}/${game.maxHp}`;endOverlay.classList.remove('hidden')}
function beginWave(){game.wave++;game.waveActive=true;let easy=LV<=1,base=easy?3:4,add=easy?1.25:1.65;game.spawnLeft=Math.ceil(base+game.wave*add+Math.max(0,LV-1));game.spawnClock=.35;game.spawnIndex=0;game.spawnPattern=[];let st=Math.floor(Math.random()*4);for(let i=0;i<game.spawnLeft;i++)game.spawnPattern.push((st+i)%4);for(let i=game.spawnPattern.length-1;i>0;i--){let j=Math.floor(Math.random()*(i+1));[game.spawnPattern[i],game.spawnPattern[j]]=[game.spawnPattern[j],game.spawnPattern[i]]}}
function pickEnemy(){let x=Math.random();if(LV<=1)return x<.50?ET[0]:x<.88?ET[1]:ET[2];if(LV===2)return x<.42?ET[0]:x<.78?ET[1]:ET[2];if(LV===3)return x<.34?ET[0]:x<.70?ET[1]:ET[2];return x<.28?ET[0]:x<.62?ET[1]:ET[2]}
function spawnEnemy(){let side=game.spawnPattern.length?game.spawnPattern[game.spawnIndex++%game.spawnPattern.length]:Math.floor(Math.random()*4),m=32,x,y;if(side===0){x=-m;y=canvas.height*(.12+Math.random()*.76)}else if(side===1){x=canvas.width+m;y=canvas.height*(.12+Math.random()*.76)}else if(side===2){x=canvas.width*(.12+Math.random()*.76);y=-m}else{x=canvas.width*(.12+Math.random()*.76);y=canvas.height+m}let easy=LV<=1,t=pickEnemy(),baseHp=easy?2.4+game.wave*.55:3.2+game.wave*.75+LV*.55,baseSp=easy?25+game.wave*1.8:30+game.wave*2.4+LV*2.5,hp=Math.max(1,Math.ceil(baseHp*t.hp)),speed=baseSp*t.sp,reward=t.reward+(easy?1:0)+Math.floor(hp/8);game.enemies.push({x,y,hp,maxHp:hp,speed,slow:0,r:t.r,reward,kind:t.name,color:t.color})}
function buildUpgrade(){let s=game.slots[game.selected],p=slotPos(game.selected),c=price(s);if(s.level>=MAX){spark(p.x,p.y,C.white,6);return}if(game.money<c){spark(p.x,p.y,C.danger,8);return}game.money-=c;if(empty(s)){s.type=game.buildType;s.level=1;s.cd=0;s.boost=0;s.flash=1;spark(p.x,p.y,TYPES[s.type].color,14)}else{s.level++;s.flash=1;spark(p.x,p.y,TYPES[s.type].color,16)}}
function special(){if(game.specialCd>0)return;let s=game.slots[game.selected],p=slotPos(game.selected);if(empty(s)){spark(p.x,p.y,C.danger,10);game.specialCd=1.5;return}s.boost=BOOST_TIME;s.flash=1.4;game.specialCd=BOOST_CD;spark(p.x,p.y,TYPES[s.type].color,30)}
function angleDiff(a,b){return Math.atan2(Math.sin(a-b),Math.cos(a-b))}function inCone(x,y,e,range,aim,cone){let dx=e.x-x,dy=e.y-y,d=Math.hypot(dx,dy);if(d>=range)return false;return Math.abs(angleDiff(Math.atan2(dy,dx),aim))<=cone}function firstInCone(x,y,range,aim,cone){let best=null,bd=range;for(const e of game.enemies){if(!inCone(x,y,e,range,aim,cone))continue;let d=Math.hypot(e.x-x,e.y-y);if(d<bd){bd=d;best=e}}return best}
function updateTowers(dt){for(let i=0;i<game.slots.length;i++){let s=game.slots[i];if(s.flash>0)s.flash-=dt*3;if(s.boost>0)s.boost-=dt;if(empty(s))continue;s.cd-=dt;let p=slotPos(i),t=TYPES[s.type],boost=s.boost>0,range=t.range+s.level*22+(boost?30:0);if(s.cd<=0){if(t.kind==='wave'){let hits=0,cone=boost?1.32:1.04;for(const e of game.enemies)if(inCone(p.x,p.y,e,range,p.a,cone)){e.hp-=t.damage*s.level*(boost?1.75:1);e.slow=Math.max(e.slow||0,boost?.55:.2);hits++}if(hits){s.cd=Math.max(.28,(t.rate-s.level*.055)*(boost?.62:1));game.shots.push({x:p.x,y:p.y,tx:p.x+Math.cos(p.a)*range,ty:p.y+Math.sin(p.a)*range,life:.22,color:t.color,beam:true});s.flash=boost?1.35:1}}else{let e=firstInCone(p.x,p.y,range,p.a,.95);if(e){s.cd=Math.max(.12,(t.rate-s.level*.075)*(boost?.48:1));if(t.kind==='slow')e.slow=(boost?2.2:1.3)+s.level*.2;e.hp-=t.damage*s.level*(boost?1.85:1);game.shots.push({x:p.x,y:p.y,tx:e.x,ty:e.y,life:.18,color:t.color,beam:t.kind==='beam'});s.flash=boost?1.3:1}}}}}
function updateEnemies(dt){for(let i=game.enemies.length-1;i>=0;i--){let e=game.enemies[i],a=Math.atan2(cy()-e.y,cx()-e.x),sp=e.speed*(e.slow>0?.52:1);e.x+=Math.cos(a)*sp*dt;e.y+=Math.sin(a)*sp*dt;e.slow=Math.max(0,e.slow-dt);if(e.hp<=0){spark(e.x,e.y,C.gold,10);game.enemies.splice(i,1);game.kills++;game.money+=e.reward||3;continue}if(Math.hypot(e.x-cx(),e.y-cy())<42){game.enemies.splice(i,1);game.coreHp--;spark(cx(),cy(),C.danger,20);if(game.coreHp<=0)endGame('The core was destroyed.')}}}
function updateWaves(dt){if(!game.waveActive){game.waveTimer-=dt;if(game.waveTimer<=0)beginWave();return}game.spawnClock-=dt;if(game.spawnLeft>0&&game.spawnClock<=0){game.spawnClock=LV<=1?.95:Math.max(.48,1.0-game.wave*.035-LV*.035);game.spawnLeft--;spawnEnemy()}if(game.spawnLeft<=0&&game.enemies.length===0){game.waveActive=false;game.waveTimer=LV<=1?3.2:2.6;if(!ENDLESS&&game.wave>=TARGET)endGame('You defended the core through every wave.',true)}}
function updateSparks(dt){for(let i=game.sparks.length-1;i>=0;i--){let p=game.sparks[i];p.x+=p.vx*dt;p.y+=p.vy*dt;p.vx*=.98;p.vy*=.98;p.life-=dt*2;if(p.life<=0)game.sparks.splice(i,1)}}function updateShots(dt){for(let i=game.shots.length-1;i>=0;i--){game.shots[i].life-=dt;if(game.shots[i].life<=0)game.shots.splice(i,1)}}
function update(dt){if(game.running){game.timer+=dt;if(game.input.topLeft){game.buildType=(game.buildType+1)%TYPES.length;game.input.topLeft=false}if(game.input.topMid){buildUpgrade();game.input.topMid=false}if(game.input.topRight){game.selected=(game.selected+1)%12;game.input.topRight=false}if(game.input.bottomLeft)game.ring-=2.4*dt;if(game.input.bottomRight)game.ring+=2.4*dt;if(game.input.bottomMid){special();game.input.bottomMid=false}game.specialCd=Math.max(0,game.specialCd-dt);updateWaves(dt);updateTowers(dt);updateEnemies(dt);updateShots(dt)}updateSparks(dt)}
function bg(){let g=ctx.createLinearGradient(0,0,canvas.width,canvas.height);g.addColorStop(0,C.bg);g.addColorStop(.6,C.bg2);g.addColorStop(1,'#080b10');ctx.fillStyle=g;ctx.fillRect(0,0,canvas.width,canvas.height);ctx.strokeStyle=C.line;ctx.lineWidth=1;for(let x=0;x<canvas.width;x+=64){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,canvas.height);ctx.stroke()}for(let y=0;y<canvas.height;y+=64){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(canvas.width,y);ctx.stroke()}}
function drawCore(){ctx.save();ctx.translate(cx(),cy());ctx.strokeStyle='rgba(255,255,255,.12)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,92,0,6.28);ctx.stroke();ctx.fillStyle='rgba(17,24,39,.95)';ctx.strokeStyle='rgba(255,255,255,.24)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,38,0,6.28);ctx.fill();ctx.stroke();ctx.fillStyle=game.coreHp<=2?C.danger:C.core;ctx.font='900 18px Arial';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(game.coreHp,0,1);ctx.restore()}
function drawSlots(){for(let i=0;i<game.slots.length;i++){let s=game.slots[i],p=slotPos(i),sel=i===game.selected;ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.a+Math.PI/2);ctx.fillStyle=sel?'rgba(248,250,252,.12)':'rgba(17,24,39,.85)';ctx.strokeStyle=sel?C.white:'rgba(255,255,255,.18)';ctx.lineWidth=sel?2.5:1.5;ctx.beginPath();ctx.arc(0,0,24+(s.flash||0)*5,0,6.28);ctx.fill();ctx.stroke();if(!empty(s)){let t=TYPES[s.type];ctx.fillStyle=t.color;ctx.beginPath();ctx.arc(0,0,13,0,6.28);ctx.fill();ctx.fillStyle='#0b0f14';ctx.font='900 11px Arial';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(s.level,0,1)}else{ctx.fillStyle='rgba(255,255,255,.22)';ctx.fillRect(-8,-2,16,4);ctx.fillRect(-2,-8,4,16)}ctx.restore()}}
function drawEnemies(){for(const e of game.enemies){ctx.fillStyle=e.slow>0?C.purple:(e.color||C.rock);ctx.strokeStyle='rgba(255,255,255,.18)';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(e.x,e.y,e.r,0,6.28);ctx.fill();ctx.stroke();if(e.kind==='large'){ctx.strokeStyle='rgba(255,255,255,.22)';ctx.beginPath();ctx.arc(e.x,e.y,e.r*.58,0,6.28);ctx.stroke()}if(e.hp<e.maxHp){let bw=Math.max(28,e.r*2.1);ctx.fillStyle='rgba(17,24,39,.8)';ctx.fillRect(e.x-bw/2,e.y-e.r-10,bw,4);ctx.fillStyle=C.green;ctx.fillRect(e.x-bw/2,e.y-e.r-10,bw*clamp(e.hp/e.maxHp,0,1),4)}}}
function drawShots(){for(const s of game.shots){ctx.globalAlpha=clamp(s.life/.18,0,1);ctx.strokeStyle=s.color;ctx.lineWidth=s.beam?4:2;ctx.beginPath();ctx.moveTo(s.x,s.y);ctx.lineTo(s.tx,s.ty);ctx.stroke();ctx.globalAlpha=1}}function drawSparks(){for(const p of game.sparks){ctx.globalAlpha=Math.max(0,p.life);ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.size*p.life,0,6.28);ctx.fill()}ctx.globalAlpha=1}
function drawButton(x,y,w,h,label,on,color){ctx.fillStyle=on?color:'rgba(17,24,39,.80)';ctx.strokeStyle=on?'rgba(255,255,255,.45)':'rgba(255,255,255,.18)';ctx.lineWidth=1.7;rr(x,y,w,h,18);ctx.fillStyle=on?'#0b0f14':C.white;ctx.font='900 12px Arial';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(label,x+w/2,y+h/2)}function topBtn(x,y,w,h,label,on,color){drawButton(x,y,w,h,'',on,color);ctx.save();ctx.translate(x+w/2,y+h/2);ctx.rotate(Math.PI);ctx.fillStyle=on?'#0b0f14':C.white;ctx.font='900 12px Arial';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(label,0,0);ctx.restore()}
function controls(){if(!game.running)return;let w=canvas.width*.31,h=56,s=game.slots[game.selected],t=TYPES[game.buildType].name,b=empty(s)?'BUILD $'+BUILD:s.level>=MAX?'MAX':'UP $'+price(s),boost='BOOST';if(s&&s.boost>0)boost='BOOST '+Math.ceil(s.boost)+'s';else if(game.specialCd>0)boost='WAIT '+Math.ceil(game.specialCd)+'s';topBtn(canvas.width*.02,76,w,h,'TYPE '+t,game.input.topLeft,C.blue);topBtn(canvas.width*.345,76,w,h,b,game.input.topMid,C.blue);topBtn(canvas.width*.67,76,w,h,'SLOT '+(game.selected+1)+'/12',game.input.topRight,C.blue);let y=canvas.height-90;drawButton(canvas.width*.02,y,w,h,'ROT ◀',game.input.bottomLeft,C.rose);drawButton(canvas.width*.345,y,w,h,boost,game.input.bottomMid,C.rose);drawButton(canvas.width*.67,y,w,h,'ROT ▶',game.input.bottomRight,C.rose)}
function drawTopHud(){if(!game.running)return;let y=24,w=112,h=30,gap=10,x1=canvas.width/2-(w*2+gap)/2,x2=x1+w+gap;function pill(x,label){ctx.save();ctx.translate(x+w/2,y+h/2);ctx.rotate(Math.PI);ctx.fillStyle='rgba(17,24,39,.82)';ctx.strokeStyle='rgba(255,255,255,.16)';ctx.lineWidth=1.4;rr(-w/2,-h/2,w,h,15);ctx.fillStyle=C.white;ctx.font='900 12px Arial';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(label,0,1);ctx.restore()}pill(x1,waveText());pill(x2,'Money $'+game.money)}
function render(){bg();drawShots();drawEnemies();drawCore();drawSlots();drawSparks();drawTopHud();controls();let hud=document.getElementById('hud');if(hud)hud.style.display='none'}
let last=performance.now();function loop(now){let dt=Math.min(.033,(now-last)/1000);last=now;update(dt);render();requestAnimationFrame(loop)}requestAnimationFrame(loop);
function visibleOverlay(){return!startOverlay.classList.contains('hidden')||!endOverlay.classList.contains('hidden')}function start(e){if(e){e.preventDefault();e.stopPropagation()}startGame(e)}[startOverlay,startButton,restartButton].forEach(el=>{el.addEventListener('touchstart',start,{passive:false});el.addEventListener('pointerdown',start);el.addEventListener('click',start)});
function keyForTouch(t){let top=t.clientY<canvas.height/2,third=t.clientX/canvas.width;if(top){if(third<.333)return'topLeft';if(third<.666)return'topMid';return'topRight'}else{if(third<.333)return'bottomLeft';if(third<.666)return'bottomMid';return'bottomRight'}}
document.addEventListener('touchstart',e=>{if(visibleOverlay())return;e.preventDefault();for(const t of e.changedTouches)game.input[keyForTouch(t)]=true},{passive:false});document.addEventListener('touchend',e=>{if(visibleOverlay())return;e.preventDefault();for(const t of e.changedTouches)game.input[keyForTouch(t)]=false},{passive:false});document.addEventListener('touchcancel',e=>{if(visibleOverlay())return;e.preventDefault();for(const t of e.changedTouches)game.input[keyForTouch(t)]=false},{passive:false});render();